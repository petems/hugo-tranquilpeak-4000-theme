name: Lint & Format

on:
    push:
        branches: [master, main, develop]
    pull_request:
        branches: [master, main, develop]
    workflow_dispatch:

jobs:
    biome:
        name: Biome lint & format check
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "22"
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Run linter
              run: npx @biomejs/biome lint assets/js/

            - name: Check formatting
              run: npx @biomejs/biome format assets/js/ --check
